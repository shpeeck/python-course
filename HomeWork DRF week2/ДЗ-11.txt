Homework DRF week 2

1. Додати токен аутентифікацію в проект

2. Додати ендпоінти за допомогою GenericViewSet:

GET /stores/ - список всіх магазинів
GET /stores/<id>> - детальна інформація про магазин
3. Додати поле owner до Store. 

4. Додати поле status(active/deactivated/in_review) до моделі Store. При створенні магазину його статус завжди in_review

5. Розробити ендпоінти в яких авторизовані користувачі можуть взаємодіяти зі списком своїх магазинів

GET /my_stores/ - список магазинів які належать користувачу
GET /my_stores/<id> - детальна інформація про магазин користувача
POST /my_stores/ - створення магазину для власника
PUT /my_stores/<id>/ - зміна даних магазину
PATCH /my_stores/<id>/ - зміна даних магазину
DELETE /my_stores/<id>/ - видалення магазину
6. Розробити ендпоінт для роботи адміністраторів та використати для нього пермішн який дозволить користуватись лише адміністраторам (user.is_staff == True) https://docs.djangoproject.com/en/4.0/ref/contrib/auth/#django.contrib.auth.models.User.is_staff 

GET /admin_stores/ - список всіх магазинів
GET /admin_stores/<id>> - детальна інформація про магазин
Додати фільтр по статусу
Додати пошук по назві
Розробити action для зміни статусу магазину POST /mark_as_active - змінює статус in_review на active. Зміна статусу тільки в такому порядку.
7. Розширити ендпоінти з пункту 5

POST /mark_as_active - змінює статус магазину з неактивного на активний
POST /mark_as_deactivated - змінює статус магазину з активного на неактивний


Завантажити завдання на git репозиторій, і прикріпити лінк на нього як відповідь